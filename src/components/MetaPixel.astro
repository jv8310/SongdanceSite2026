---
// Meta Pixel base code: load fbevents.js, init the pixel, fire PageView.
// Rendered on every page that needs Pixel tracking (the registration pages +
// the post-checkout success page) so that custom/standard events
// (InitiateCheckout, Purchase) can call window.fbq afterwards. Renders nothing
// when no pixel id is configured.
interface Props {
  pixelId: string;
}
const { pixelId } = Astro.props;
---
{pixelId && (
  <script is:inline set:html={`!function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,document,'script','https://connect.facebook.net/en_US/fbevents.js');fbq('init','${pixelId}');fbq('track','PageView');`} />
)}
